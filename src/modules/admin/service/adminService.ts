import type { AdminHomeData } from '../@types/admin';

export function loadAdminHomeData(): AdminHomeData {
  return {
    title: 'Cooperativa',
    description: 'Módulo do administrador em breve.',
  };
}
